apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: us-central1-docker.pkg.dev/ecom-project-468216/ecomrepo/order-service:v1
          ports:
          - containerPort: 8080
          env:
            - name: AUTH_SERVICE_URL
              value: "http://auth-service.default.svc.cluster.local/auth/validate"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"

